def RemoveDuplicates(head):
  pointer==head
  if head is not None:
    while pointer.next is not None:
      if pointer.val=pointer.next.val:
        pointer.next=point.val.val
      else:
        pointer=pointer.next
  return head


    
      
      
